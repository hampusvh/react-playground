

function SearchBar ({ SearchBar }) {

    return (
        <div>
            <input type="text"
            placeholder="search">
            
            <button
            onClick={search}>Search</button>
            </input>
        </div>
    );

}

export detfault SearchBar;